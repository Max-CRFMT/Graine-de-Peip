using UnityEngine;
//using TM_pro;

public class PseudoLengthHandler : MonoBehaviour
{
    //[SerializeField] TMP_InputField PseudoJoueur;

    //Si on a plus de 12 caractères, alors on n'ajoute pas de caractères au pseudo et on le garde comme avant.
}
